# Alpha Shop — Gold / Silver

این نسخه دو سرویس مستقل دارد:

- Gold: پنل قبلی + قیمت حجم دلخواه ۴۰۰۰ تومان به ازای هر GB
- Silver: پنل دوم + قیمت حجم دلخواه ۲۰۰۰ تومان به ازای هر GB
- پلن‌های ثابت هر دو سرویس: ۵، ۱۰، ۲۰ و ۴۰ گیگ
- تست رایگان Gold و Silver جداگانه و هرکدام یک‌بار برای هر کاربر

## پنل‌ها

Gold از تنظیمات `GOLD_PANEL_*` استفاده می‌کند و Silver از `SILVER_PANEL_*`.
پنل Silver در `.env` همین پروژه تنظیم شده است.

اگر پنل Silver گروه پیش‌فرض متفاوتی دارد، مقدار `DEFAULT_GROUP_ID` یا `group_ids` داخل `panel.py` را مطابق پنل تغییر بده.

## نصب

```bash
pip install -r requirements.txt
python3 bot.py
```

`python-telegram-bot` باید نسخه 22.7 یا بالاتر و کمتر از 23 باشد تا استایل رنگی دکمه‌ها (`primary`, `success`, `danger`) فعال باشد.

## ساختار خرید

خرید سرویس → انتخاب Gold/Silver → انتخاب حجم یا حجم دلخواه → تأیید → ساخت کاربر در پنل مربوط به همان سرویس.

## نکته Premium Emoji

استایل رنگی دکمه‌ها بدون Premium قابل استفاده است. Custom Emoji داخل دکمه‌ها طبق محدودیت Telegram برای باتی که مالک Premium ندارد قابل تضمین نیست؛ بنابراین نسخه فعلی از ایموجی‌های معمولی و استایل رنگی رسمی Telegram استفاده می‌کند.
